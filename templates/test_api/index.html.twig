{% extends 'base.html.twig' %}

{% block title %}
    Hello TestApiController!
{% endblock %}

{% block body %}
    <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class="example-wrapper">
        <h1>
            Hello
            {{ controller_name }}
            ! ✅
        </h1>

       

        {% if todoDTO is defined and todoDTO != null %}
           <p> Title:
           {{ todoDTO.title }}</p>
            <p> Id:
           {{ todoDTO.id }}</p>
            <p> CreatedAt:
           {{ todoDTO.createdAt|date('Y-m-d H:i:s')}}</p>
             <p> UpdatedAt:
           {{ todoDTO.updatedAt|date('Y-m-d H:i:s')}}</p>
            
        {% endif %}

    </div>
{% endblock %}
